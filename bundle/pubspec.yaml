name: device_vendor_info
description: Get hardware information (BIOS, Motherboard and System) into Flutter platform.
version: 1.0.0
homepage: https://github.com/rk0cc/device_vendor_info
funding:
  - https://github.com/sponsors/rk0cc
topics:
  - hardware
  - device
  - bios
environment: 
  sdk: '>=3.2.0 <4.0.0'
  flutter: '>=3.16.0'
dependencies:
  flutter:
    sdk: flutter
  meta: ^1.10.0
  device_vendor_info_interface: ^4.1.1
  device_vendor_info_unix: ^1.0.0
  device_vendor_info_windows: ^1.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
platforms:
  linux:
  macos:
  windows: 
