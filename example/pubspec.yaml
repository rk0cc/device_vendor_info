name: device_vendor_info_example
description: "Demo Flutter application for fetching hardware informations"
publish_to: 'none'
version: 3.0.0
resolution: workspace
environment:
  sdk: ^3.10.0
dependencies:
  flutter:
    sdk: flutter
  device_vendor_info: ^3.0.0
  intl: '>=0.20.0'
dev_dependencies:
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
